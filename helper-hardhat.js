const MIN_DELAY = 3600;
const VOTING_DELAY = 1;
const LOCAL_CHAIN = ["hardhat", "localhost"];
const REASON = "Proposal #1: Create new leader";
const VOTING_PERIOD = 5;

const QUORUM_PERCENTAGE = 4;

module.exports = {
  MIN_DELAY,
  VOTING_DELAY,
  LOCAL_CHAIN,
  REASON,
  VOTING_PERIOD,
  QUORUM_PERCENTAGE,
};
